#include <bits/stdc++.h>
using namespace std;

constexpr int INF = 1000000000;
int main(){
    int N;
    cin >> N;

    vector<int> scaffold(N+1,INF);
    for (int i=0;i<N;i++) cin >> scaffold[i];

    vector<int> dp(N+1,INF);
    dp[0] = 0;
    int i=0;
    while(i < N){
    }
}
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/ABC_299/B",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}